# prompt-agent

- Keep code minimal and explicit.
- Reuse `common` infrastructure instead of duplicating logging, config, registry, or storage setup.
- Fail loudly for missing providers, missing API keys, missing placeholders, and missing files.
